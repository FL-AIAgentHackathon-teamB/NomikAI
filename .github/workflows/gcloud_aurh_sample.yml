name: Google Cloud Auth Sample

on:
  workflow_dispatch:
jobs:
  job_id:
    # Any runner supporting Node 20 or newer
    runs-on: ubuntu-latest

    # Add "id-token" with the intended permissions.
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - uses: 'actions/checkout@v4'

    - uses: 'google-github-actions/auth@v3'
      with:
        project_id: ${{ vars.GCP_PROJECT_ID }}
        workload_identity_provider: 'projects/38689850492/locations/global/workloadIdentityPools/terraform-oidc/providers/terraform-oidc'
